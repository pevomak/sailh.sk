import React, { useState, useEffect, createContext, useContext } from 'react';
import { HashRouter, Routes, Route, Link, useLocation } from 'react-router-dom';
import {
  Activity,
  Calendar,
  ChevronDown,
  ChevronRight,
  Facebook,
  Globe,
  Home,
  Info,
  Mail,
  MapPin,
  Menu,
  Moon,
  Phone,
  Rss,
  Shield,
  Sparkles,
  Sun,
  Trophy,
  Users,
  X,
  Youtube,
  Handshake,
  Newspaper,
  Medal,
  CalendarDays,
  Send,
  Building,
  Star,
  ArrowUp,
  FileText, // Pridané pre oficiálne údaje
  Briefcase, // Pridané pre oficiálne údaje
  Hash, // Pridané pre oficiálne údaje
} from 'lucide-react';

// --- Globálny štýl a import písma ---
const GlobalStyle = () => (
  <style>{`
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap');
    body {
      font-family: 'Inter', sans-serif;
    }
  `}</style>
);

// --- Komponenty Shadcn/ui (Zjednodušené pre jeden súbor) ---
// Tieto komponenty sú zvyčajne v samostatných súboroch, ale tu ich definujeme priamo.

// Utility pre Shadcn
const cn = (...inputs) => {
  return inputs.filter(Boolean).join(' ');
};

// Button Komponent
const Button = React.forwardRef(({ className, variant = 'default', size = 'default', asChild = false, ...props }, ref) => {
  const Comp = asChild ? 'span' : 'button'; // Použijeme span pre asChild, keďže Slot nie je dostupný
  const variants = {
    default: "bg-blue-700 text-white hover:bg-blue-800 dark:bg-blue-600 dark:hover:bg-blue-700",
    destructive: "bg-red-600 text-white hover:bg-red-700 dark:bg-red-700 dark:hover:bg-red-800",
    outline: "border border-gray-300 hover:bg-gray-100 dark:border-gray-700 dark:hover:bg-gray-800",
    secondary: "bg-gray-200 text-gray-800 hover:bg-gray-300 dark:bg-gray-700 dark:text-gray-100 dark:hover:bg-gray-600",
    ghost: "hover:bg-gray-100 dark:hover:bg-gray-800",
    link: "text-blue-700 underline-offset-4 hover:underline dark:text-blue-400",
  };
  const sizes = {
    default: 'h-10 px-4 py-2',
    sm: 'h-9 rounded-md px-3',
    lg: 'h-11 rounded-md px-8',
    icon: 'h-10 w-10',
  };
  return (
    <Comp
      className={cn(
        'inline-flex items-center justify-center rounded-md text-sm font-medium ring-offset-white transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50',
        variants[variant],
        sizes[size],
        className
      )}
      ref={ref}
      {...props}
    />
  );
});

// Card Komponenty
const Card = React.forwardRef(({ className, ...props }, ref) => (
  <div
    ref={ref}
    className={cn('rounded-xl border bg-white text-gray-900 shadow-lg dark:bg-gray-800 dark:border-gray-700 dark:text-gray-50', className)}
    {...props}
  />
));

const CardHeader = React.forwardRef(({ className, ...props }, ref) => (
  <div ref={ref} className={cn('flex flex-col space-y-1.5 p-6', className)} {...props} />
));

const CardTitle = React.forwardRef(({ className, children, ...props }, ref) => (
  <h3 ref={ref} className={cn('font-semibold leading-none tracking-tight text-2xl', className)} {...props}>
    {children}
  </h3>
));

const CardDescription = React.forwardRef(({ className, ...props }, ref) => (
  <p ref={ref} className={cn('text-sm text-gray-600 dark:text-gray-400', className)} {...props} />
));

const CardContent = React.forwardRef(({ className, ...props }, ref) => (
  <div ref={ref} className={cn('p-6 pt-0', className)} {...props} />
));

const CardFooter = React.forwardRef(({ className, ...props }, ref) => (
  <div ref={ref} className={cn('flex items-center p-6 pt-0', className)} {...props} />
));

// Accordion Komponenty
const AccordionContext = React.createContext({});
const useAccordion = () => React.useContext(AccordionContext);

const Accordion = ({ children, type = 'single', collapsible = false, ...props }) => {
  const [value, setValue] = useState(type === 'single' ? '' : []);

  const toggleValue = (itemValue) => {
    if (type === 'single') {
      setValue(collapsible && value === itemValue ? '' : itemValue);
    } else {
      setValue((prev) =>
        prev.includes(itemValue)
          ? prev.filter((v) => v !== itemValue)
          : [...prev, itemValue]
      );
    }
  };

  return (
    <AccordionContext.Provider value={{ value, toggleValue, type }}>
      <div {...props}>{children}</div>
    </AccordionContext.Provider>
  );
};

const AccordionItemContext = React.createContext('');

const AccordionItem = React.forwardRef(({ children, className, value, ...props }, ref) => (
  <AccordionItemContext.Provider value={value}>
    <div ref={ref} className={cn('border-b dark:border-gray-700', className)} {...props}>
      {children}
    </div>
  </AccordionItemContext.Provider>
));

const AccordionTrigger = React.forwardRef(({ children, className, ...props }, ref) => {
  const { value, toggleValue, type } = useAccordion();
  const itemValue = React.useContext(AccordionItemContext);
  const isOpen = type === 'single' ? value === itemValue : value.includes(itemValue);

  return (
    <button
      ref={ref}
      className={cn(
        'flex flex-1 items-center justify-between py-4 font-medium transition-all hover:underline [&[data-state=open]>svg]:rotate-180 dark:hover:text-gray-300',
        className
      )}
      data-state={isOpen ? 'open' : 'closed'}
      onClick={() => toggleValue(itemValue)}
      {...props}
    >
      {children}
      <ChevronDown className="h-4 w-4 shrink-0 transition-transform duration-200" />
    </button>
  );
});

const AccordionContent = React.forwardRef(({ children, className, ...props }, ref) => {
  const { value, type } = useAccordion();
  const itemValue = React.useContext(AccordionItemContext);
  const isOpen = type === 'single' ? value === itemValue : value.includes(itemValue);

  if (!isOpen) return null;

  return (
    <div
      ref={ref}
      className={cn(
        'overflow-hidden text-sm transition-all data-[state=closed]:animate-accordion-up data-[state=open]:animate-accordion-down',
        className
      )}
      data-state={isOpen ? 'open' : 'closed'}
      {...props}
    >
      <div className="pb-4 pt-0">{children}</div>
    </div>
  );
});

// Input Komponent
const Input = React.forwardRef(({ className, type, ...props }, ref) => {
  return (
    <input
      type={type}
      className={cn(
        'flex h-10 w-full rounded-md border border-gray-300 bg-white px-3 py-2 text-sm ring-offset-white file:border-0 file:bg-transparent file:text-sm file:font-medium placeholder:text-gray-500 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-50 dark:bg-gray-900 dark:border-gray-700 dark:text-gray-50 dark:placeholder:text-gray-500 dark:focus-visible:ring-blue-600',
        className
      )}
      ref={ref}
      {...props}
    />
  );
});

// Textarea Komponent
const Textarea = React.forwardRef(({ className, ...props }, ref) => {
  return (
    <textarea
      className={cn(
        'flex min-h-[80px] w-full rounded-md border border-gray-300 bg-white px-3 py-2 text-sm ring-offset-white placeholder:text-gray-500 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-50 dark:bg-gray-900 dark:border-gray-700 dark:text-gray-50 dark:placeholder:text-gray-500 dark:focus-visible:ring-blue-600',
        className
      )}
      ref={ref}
      {...props}
    />
  );
});

// --- Konfigurácia Prekladov (vlastný systém namiesto i18next) ---
const translations = {
  sk: {
    translation: {
      // Navigácia
      NAV_HOME: 'Domov',
      NAV_ABOUT: 'O nás',
      NAV_COMPETITIONS: 'Súťaže',
      NAV_EXTRALIGA: 'Extraliga (Kluby)',
      NAV_IAL: 'Amatérska Liga (IAL)',
      NAV_SUCCESSES: 'Úspechy',
      NAV_PARTNERS: 'Partneri',
      NAV_MEDIA: 'Médiá',
      NAV_CALENDAR: 'Kalendár',
      NAV_CONTACT: 'Kontakt',
      // Hero sekcia
      HERO_TITLE: 'Inline Hokej <span class="text-red-600">Slovensko</span>',
      HERO_SUBTITLE: 'Oficiálny domov Slovenskej asociácie in-line hokeja (SAILH). Prinášame rýchlosť, vášeň a rozvoj talentov na kolesách.',
      HERO_BUTTON: 'Zistiť viac o nás',
      // Domovská stránka - Aktuality
      HOME_NEWS_TITLE: 'Najnovšie zo SAILH',
      NEWS_1_CATEGORY: 'MAJSTROVSTVÁ SVETA 2024 / 2025',
      NEWS_1_TITLE: 'U19 a X-Style Legends sú Majstri Sveta!',
      NEWS_1_DESC: 'Na MS 2024 získala kategória U19 titul majstrov sveta. V roku 2025 kategória X-Style Legends vybojovala prestížny titul majstrov sveta.',
      NEWS_2_CATEGORY: 'MAJSTROVSTVÁ EURÓPY 2025',
      NEWS_2_TITLE: 'Bronz pre U19 a 4. miesto pre U17',
      NEWS_2_DESC: 'Na ME 2025 sme ukázali silu našej mládeže. Kategória U17 obsadila 4. miesto a kategória U19 získala bronzové medaily.',
      READ_MORE: 'Čítať viac',
      LINK_IAL: 'In-Line Amatérska Liga',
      LINK_MIHWA: 'MS Legendy (MIHWA)',
      // Domovská stránka - RSS
      RSS_TITLE: 'Svetové Novinky (MIHWA)',
      RSS_LOADING: 'Načítavam RSS feed...',
      RSS_ERROR: 'Nepodarilo sa načítať RSS feed. Skúste to prosím neskôr.',
      RSS_SOURCE: 'Zdroj: MIHWA.org',
      // Domovská stránka - Video
      VIDEO_TITLE: 'Highlight Týždňa',
      VIDEO_DESC: 'Záznam zápasu: NITRA HOCKEY TEAM vs ILK ROLLER HAMMERS BRATISLAVA.', // Aktualizované
      // Stránka - O nás
      ABOUT_TITLE: 'O Slovenskej asociácii in-line hokeja (SAILH)',
      ABOUT_P1: 'Slovenská asociácia in-line hokeja (SAILH) je <strong>hlavným riadiacim orgánom</strong> pre in-line hokej na Slovensku. Našou primárnou misiou je rozvoj tohto dynamického športu na všetkých úrovniach – od náborov mládeže až po prípravu národnej reprezentácie na medzinárodné súťaže.',
      ABOUT_MISSION_TITLE: 'Misia a Vízia',
      ABOUT_MISSION: 'Naša Misia',
      ABOUT_MISSION_TEXT: 'Propagovať a rozvíjať in-line hokej na Slovensku, vytvárať podmienky pre rast talentov a zabezpečiť úspešnú reprezentáciu na medzinárodnej scéne.',
      ABOUT_VISION: 'Naša Vízia',
      ABOUT_VISION_TEXT: 'Stať sa lídrom v in-line hokeji v strednej Európe, s plnohodnotnými súťažami a pravidelnými účasťami na svetových šampionátoch s cieľom získavať medaily.',
      ABOUT_PRIORITIES_TITLE: 'Naše priority',
      ABOUT_PRIORITY_1_TITLE: 'Mládež a nábor',
      ABOUT_PRIORITY_1_TEXT: 'Budovanie silnej základne prostredníctvom regionálnych škôl a kempov.',
      ABOUT_PRIORITY_2_TITLE: 'Ligy a Súťaže',
      ABOUT_PRIORITY_2_TEXT: 'Zabezpečenie profesionálnej a férovej súťažnej štruktúry pre všetky vekové kategórie.',
      ABOUT_PRIORITY_3_TITLE: 'Reprezentácia',
      ABOUT_PRIORITY_3_TEXT: 'Účasť na medzinárodných podujatiach, vrátane World Skate Games, s cieľom dosiahnuť čo najlepšie výsledky.',
      // Nové preklady pre oficiálne údaje
      ABOUT_OFFICIAL_DATA_TITLE: 'Oficiálne údaje',
      ABOUT_LEGAL_NAME: 'Názov združenia',
      ABOUT_LEGAL_NAME_VALUE: 'Slovenská asociácia inline hokeja',
      ABOUT_LEGAL_FORM: 'Právna forma',
      ABOUT_LEGAL_FORM_VALUE: 'Občianske združenie, odborová organizácia a organizácia zamestnávateľov',
      ABOUT_ICO: 'IČO',
      ABOUT_ICO_VALUE: '57133671',
      ABOUT_ADDRESS: 'Sídlo',
      ABOUT_ADDRESS_VALUE: 'Oreganová 1178/1, 900 46 Most pri Bratislave - Studené',
      ABOUT_REG_NUM: 'Registračné číslo',
      ABOUT_REG_NUM_VALUE: 'VVS/1-900/90-72711',
      ABOUT_REG_AUTH: 'Registrový úrad',
      ABOUT_REG_AUTH_VALUE: 'Okresný úrad Bratislava',
      ABOUT_DATE: 'Dátum vzniku',
      ABOUT_DATE_VALUE: '12.08.2025',
      // Stránka - Kluby (Extraliga)
      EXTRALIGA_TITLE: 'Kluby Extraligy',
      EXTRALIGA_DESC: 'Najvyššia slovenská in-line hokejová súťaž. Extraliga predstavuje špičku tohto športu v SR. Aktuálne výsledky, štatistiky a tabuľky sú pravidelne aktualizované.',
      TABLE_RANK: 'Poradie',
      TABLE_TEAM: 'Tím',
      TABLE_GAMES: 'Z',
      TABLE_POINTS: 'B',
      TEAM_1_NAME: 'Roller Hammers Bratislava',
      TEAM_2_NAME: 'Devils Zvolen',
      TEAM_3_NAME: 'ŠK 98 Pruské',
      TEAM_4_NAME: 'Rockets Most p.B.',
      // Stránka - IAL
      IAL_TITLE: 'Amatérska In-line Hokejová Liga (IAL)',
      IAL_DESC: 'IAL je srdcom regionálneho in-line hokeja. Spája nadšencov všetkých vekových kategórií a podporuje zdravú súťaživosť a komunitu.',
      IAL_NOTE: 'Údaje o zápasoch sú simulované na základe posledných informácií z inlineliga.sk. Pre aktuálne info navštívte ich web.',
      IAL_CURRENT_MATCHES: 'Aktuálne Zápasy (Simulované)',
      IAL1_TITLE: 'IAL 1 (8. kolo)',
      IAL_TIME: 'Čas',
      IAL_MATCH: 'Zápas',
      IAL_DATE_1: 'Nedeľa, 9. nov 2025',
      IAL1_MATCH_1: 'Old Boys Viničné vs. Wildthing Karaoke',
      IAL1_MATCH_2: 'Team 300 vs. Pressburg Kings',
      IAL_BYE: 'Voľno',
      IAL_TEAM_BEEWARE: 'BeeWare',
      IAL2_TITLE: 'IAL 2 (8. kolo)',
      IAL_DATE_2: 'Streda, 5. nov 2025',
      IAL2_MATCH_1: 'BeeWare vs. Mullets',
      IAL2_MATCH_2: 'Chiefs vs. Vikings',
      // Stránka - Úspechy
      SUCCESSES_TITLE: 'Naše najväčšie úspechy',
      SUCCESSES_DESC: 'Sme hrdí na úspechy našich tímov na medzinárodnej scéne. Tu sú niektoré z historických momentov slovenského inline hokeja.',
      SUCCESS_1_CATEGORY: 'WORLD SKATE GAMES | JUNIORI',
      SUCCESS_1_TITLE: 'Historické Zlato! Slovenskí juniori sú Majstri Sveta!',
      SUCCESS_1_P1: 'Mladí slovenskí in-line hokejisti sa stali juniorskými majstrami sveta. Vo finále Svetových korčuliarskych hier v talianskom Abruzze zvíťazili nad Španielskom po veľkom boji 4:3 po predĺžení.',
      SUCCESS_1_P2: 'Na ceste za titulom si poradili s Austráliou, Čínou, Kolumbiou, Francúzskom a Talianskom. Maxima Šimku vyhlásili za najužitočnejšieho hráča a Lucu Ferkodiča za najlepšieho brankára.',
      SUCCESS_2_CATEGORY: 'MAJSTROVSTVÁ EURÓPY | SENIORI',
      SUCCESS_2_TITLE: 'Seniorský tím získal Striebro na ME',
      SUCCESS_2_P1: 'Slovenská seniorská reprezentácia dosiahla fantastický úspech, keď si vybojovala strieborné medaily. Vo finále podľahli silnému tímu Fínska tesne 2:3.',
      // Stránka - Partneri
      PARTNERS_TITLE: 'Partneri SAILH',
      PARTNERS_DESC: 'Rozvoj slovenského in-line hokeja by nebol možný bez silnej podpory našich partnerov. Ďakujeme im za dôveru a spoluprácu.',
      PARTNERS_GENERAL: 'Generálny partner',
      PARTNERS_MAIN: 'Hlavní partneri',
      PARTNERS_MEDIA: 'Mediálni partneri',
      PARTNERS_CTA_TITLE: 'Staňte sa partnerom',
      PARTNERS_CTA_DESC: 'Podporte s nami dynamický šport s rastúcou základňou. Kontaktujte nás a spoločne nájdeme ideálny model spolupráce.',
      PARTNERS_CTA_BUTTON: 'Mám záujem o partnerstvo',
      // Stránka - Médiá
      MEDIA_TITLE: 'Médiá a Galéria',
      MEDIA_DESC: 'Pozrite si najnovšie videá, zostrihy zápasov a vizuálne materiály z akcií a turnajov SAILH.',
      MEDIA_VIDEO_GALLERY: 'Záznamy Zápasov (Extraliga)', // Aktualizované
      MEDIA_POSTERS_GALLERY: 'Plagáty a Grafika',
      // Stránka - Kalendár
      CALENDAR_TITLE: 'Kalendár Podujatí',
      CALENDAR_DESC: 'Oficiálny kalendár SAILH. Tu nájdete všetky dôležité termíny Extraligy, reprezentačných zrazov, medzinárodných turnajov a mládežníckych kempov.',
      // Stránka - Kontakt
      CONTACT_TITLE: 'Kontakt a Sociálne siete',
      CONTACT_FORM_TITLE: 'Napíšte nám',
      CONTACT_FORM_NAME: 'Meno a Priezvisko',
      CONTACT_FORM_EMAIL: 'Váš E-mail',
      CONTACT_FORM_MESSAGE: 'Správa',
      CONTACT_FORM_SUBMIT: 'Odoslať správu',
      CONTACT_FORM_SUCCESS: 'Ďakujeme za správu! Čoskoro sa vám ozveme.',
      CONTACT_DETAILS_TITLE: 'Kontaktné údaje',
      CONTACT_ADDRESS: 'Oreganová 1178/1, 900 46 Most pri Bratislave - Studené', // Aktualizované
      CONTACT_PHONE: '+421 905 627 715',
      CONTACT_EMAIL: 'pejchalr@gmail.com',
      CONTACT_SOCIAL_TITLE: 'Novinky z Facebooku',
      // Pätička
      FOOTER_ABOUT: 'Slovenská asociácia in-line hokeja. Oficiálny riadiaci orgán pre in-line hokej v SR.',
      FOOTER_NAV_TITLE: 'Rýchla Navigácia',
      FOOTER_CONTACT_TITLE: 'Kontakt',
      FOOTER_COPYRIGHT: '© 2025 Slovenská asociácia in-line hokeja (SAILH). Všetky práva vyhradené.',
    }
  },
  en: {
    translation: {
      // Navigácia
      NAV_HOME: 'Home',
      NAV_ABOUT: 'About Us',
      NAV_COMPETITIONS: 'Competitions',
      NAV_EXTRALIGA: 'Extraliga (Clubs)',
      NAV_IAL: 'Amateur League (IAL)',
      NAV_SUCCESSES: 'Successes',
      NAV_PARTNERS: 'Partners',
      NAV_MEDIA: 'Media',
      NAV_CALENDAR: 'Calendar',
      NAV_CONTACT: 'Contact',
      // Hero sekcia
      HERO_TITLE: 'Inline Hockey <span class="text-red-600">Slovakia</span>',
      HERO_SUBTITLE: 'The official home of the Slovak Inline Hockey Association (SAILH). Delivering speed, passion, and talent development on wheels.',
      HERO_BUTTON: 'Learn more about us',
      // Domovská stránka - Aktuality
      HOME_NEWS_TITLE: 'Latest from SAILH',
      NEWS_1_CATEGORY: 'WORLD CHAMPIONSHIPS 2024 / 2025',
      NEWS_1_TITLE: 'U19 and X-Style Legends are World Champions!',
      NEWS_1_DESC: 'At the 2024 World Champs, the U19 category won the world title. In 2025, the X-Style Legends category won the prestigious world title.',
      NEWS_2_CATEGORY: 'EUROPEAN CHAMPIONSHIPS 2025',
      NEWS_2_TITLE: 'Bronze for U19 and 4th place for U17',
      NEWS_2_DESC: 'At the 2025 European Champs, we showed the strength of our youth. The U17 category took 4th place and the U19 category won bronze medals.',
      READ_MORE: 'Read More',
      LINK_IAL: 'In-Line Amateur League',
      LINK_MIHWA: 'Legends Worlds (MIHWA)',
      // Domovská stránka - RSS
      RSS_TITLE: 'World News (MIHWA)',
      RSS_LOADING: 'Loading RSS feed...',
      RSS_ERROR: 'Failed to load RSS feed. Please try again later.',
      RSS_SOURCE: 'Source: MIHWA.org',
      // Domovská stránka - Video
      VIDEO_TITLE: 'Highlight of the Week',
      VIDEO_DESC: 'Match recording: NITRA HOCKEY TEAM vs ILK ROLLER HAMMERS BRATISLAVA.', // Aktualizované
      // Stránka - O nás
      ABOUT_TITLE: 'About the Slovak Inline Hockey Association (SAILH)',
      ABOUT_P1: 'The Slovak Inline Hockey Association (SAILH) is the <strong>main governing body</strong> for inline hockey in Slovakia. Our primary mission is the development of this dynamic sport at all levels – from youth recruitment to preparing the national team for international competitions.',
      ABOUT_MISSION_TITLE: 'Mission and Vision',
      ABOUT_MISSION: 'Our Mission',
      ABOUT_MISSION_TEXT: 'To promote and develop inline hockey in Slovakia, create conditions for talent growth, and ensure successful representation on the international stage.',
      ABOUT_VISION: 'Our Vision',
      ABOUT_VISION_TEXT: 'To become a leader in inline hockey in Central Europe, with full-fledged competitions and regular participation in world championships with the aim of winning medals.',
      ABOUT_PRIORITIES_TITLE: 'Our Priorities',
      ABOUT_PRIORITY_1_TITLE: 'Youth and Recruitment',
      ABOUT_PRIORITY_1_TEXT: 'Building a strong foundation through regional schools and camps.',
      ABOUT_PRIORITY_2_TITLE: 'Leagues and Competitions',
      ABOUT_PRIORITY_2_TEXT: 'Ensuring a professional and fair competitive structure for all age categories.',
      ABOUT_PRIORITY_3_TITLE: 'National Team',
      ABOUT_PRIORITY_3_TEXT: 'Participation in international events, including the World Skate Games, with the aim of achieving the best results.',
      // Nové preklady pre oficiálne údaje
      ABOUT_OFFICIAL_DATA_TITLE: 'Official Information',
      ABOUT_LEGAL_NAME: 'Association Name',
      ABOUT_LEGAL_NAME_VALUE: 'Slovenská asociácia inline hokeja',
      ABOUT_LEGAL_FORM: 'Legal Form',
      ABOUT_LEGAL_FORM_VALUE: 'Civic association, trade union organization and employers\' organization',
      ABOUT_ICO: 'Business ID (IČO)',
      ABOUT_ICO_VALUE: '57133671',
      ABOUT_ADDRESS: 'Headquarters',
      ABOUT_ADDRESS_VALUE: 'Oreganová 1178/1, 900 46 Most pri Bratislave - Studené',
      ABOUT_REG_NUM: 'Registration Number',
      ABOUT_REG_NUM_VALUE: 'VVS/1-900/90-72711',
      ABOUT_REG_AUTH: 'Registry Authority',
      ABOUT_REG_AUTH_VALUE: 'Okresný úrad Bratislava',
      ABOUT_DATE: 'Date of Establishment',
      ABOUT_DATE_VALUE: '12.08.2025',
      // Stránka - Kluby (Extraliga)
      EXTRALIGA_TITLE: 'Extraliga Clubs',
      EXTRALIGA_DESC: 'The highest Slovak inline hockey competition. Extraliga represents the pinnacle of this sport in Slovakia. Current results, statistics, and tables are regularly updated.',
      TABLE_RANK: 'Rank',
      TABLE_TEAM: 'Team',
      TABLE_GAMES: 'G',
      TABLE_POINTS: 'P',
      TEAM_1_NAME: 'Roller Hammers Bratislava',
      TEAM_2_NAME: 'Devils Zvolen',
      TEAM_3_NAME: 'ŠK 98 Pruské',
      TEAM_4_NAME: 'Rockets Most p.B.',
      // Stránka - IAL
      IAL_TITLE: 'Amateur Inline Hockey League (IAL)',
      IAL_DESC: 'IAL is the heart of regional inline hockey. It brings together enthusiasts of all ages and promotes healthy competition and community.',
      IAL_NOTE: 'Match data is simulated based on the latest information from inlineliga.sk. For current info, please visit their website.',
      IAL_CURRENT_MATCHES: 'Current Matches (Simulated)',
      IAL1_TITLE: 'IAL 1 (Round 8)',
      IAL_TIME: 'Time',
      IAL_MATCH: 'Match',
      IAL_DATE_1: 'Sunday, Nov 9, 2025',
      IAL1_MATCH_1: 'Old Boys Viničné vs. Wildthing Karaoke',
      IAL1_MATCH_2: 'Team 300 vs. Pressburg Kings',
      IAL_BYE: 'Bye',
      IAL_TEAM_BEEWARE: 'BeeWare',
      IAL2_TITLE: 'IAL 2 (Round 8)',
      IAL_DATE_2: 'Wednesday, Nov 5, 2025',
      IAL2_MATCH_1: 'BeeWare vs. Mullets',
      IAL2_MATCH_2: 'Chiefs vs. Vikings',
      // Stránka - Úspechy
      SUCCESSES_TITLE: 'Our Greatest Successes',
      SUCCESSES_DESC: 'We are proud of our teams\' achievements on the international stage. Here are some of the historic moments of Slovak inline hockey.',
      SUCCESS_1_CATEGORY: 'WORLD SKATE GAMES | JUNIORS',
      SUCCESS_1_TITLE: 'Historic Gold! Slovak Juniors are World Champions!',
      SUCCESS_1_P1: 'The young Slovak inline hockey players became junior world champions. In the final of the World Skate Games in Abruzzo, Italy, they defeated Spain 4:3 in overtime.',
      SUCCESS_1_P2: 'On their way to the title, they defeated Australia, China, Colombia, France, and Italy. Maxima Šimku was named MVP, and Luca Ferkodič was named Best Goalkeeper.',
      SUCCESS_2_CATEGORY: 'EUROPEAN CHAMPIONSHIPS | SENIORS',
      SUCCESS_2_TITLE: 'Senior Team Wins Silver at European Championships',
      SUCCESS_2_P1: 'The Slovak senior national team achieved fantastic success, winning silver medals. In the final, they narrowly lost to a strong Finnish team 2:3.',
      // Stránka - Partneri
      PARTNERS_TITLE: 'SAILH Partners',
      PARTNERS_DESC: 'The development of Slovak inline hockey would not be possible without the strong support of our partners. We thank them for their trust and cooperation.',
      PARTNERS_GENERAL: 'General Partner',
      PARTNERS_MAIN: 'Main Partners',
      PARTNERS_MEDIA: 'Media Partners',
      PARTNERS_CTA_TITLE: 'Become a Partner',
      PARTNERS_CTA_DESC: 'Support a dynamic sport with a growing base. Contact us, and together we will find the ideal cooperation model.',
      PARTNERS_CTA_BUTTON: 'I am interested in partnership',
      // Stránka - Médiá
      MEDIA_TITLE: 'Media and Gallery',
      MEDIA_DESC: 'Check out the latest videos, match highlights, and visual materials from SAILH events and tournaments.',
      MEDIA_VIDEO_GALLERY: 'Match Recordings (Extraliga)', // Aktualizované
      MEDIA_POSTERS_GALLERY: 'Posters and Graphics',
      // Stránka - Kalendár
      CALENDAR_TITLE: 'Event Calendar',
      CALENDAR_DESC: 'Official SAILH calendar. Here you will find all important dates for Extraliga, national team gatherings, international tournaments, and youth camps.',
      // Stránka - Kontakt
      CONTACT_TITLE: 'Contact and Social Media',
      CONTACT_FORM_TITLE: 'Write to Us',
      CONTACT_FORM_NAME: 'Full Name',
      CONTACT_FORM_EMAIL: 'Your E-mail',
      CONTACT_FORM_MESSAGE: 'Message',
      CONTACT_FORM_SUBMIT: 'Send Message',
      CONTACT_FORM_SUCCESS: 'Thank you for your message! We will get back to you shortly.',
      CONTACT_DETAILS_TITLE: 'Contact Details',
      CONTACT_ADDRESS: 'Oreganová 1178/1, 900 46 Most pri Bratislave - Studené', // Aktualizované
      CONTACT_PHONE: '+421 905 627 715',
      CONTACT_EMAIL: 'pejchalr@gmail.com',
      CONTACT_SOCIAL_TITLE: 'Facebook Newsfeed',
      // Pätička
      FOOTER_ABOUT: 'Slovak Inline Hockey Association. The official governing body for inline hockey in Slovakia.',
      FOOTER_NAV_TITLE: 'Quick Navigation',
      FOOTER_CONTACT_TITLE: 'Contact',
      FOOTER_COPYRIGHT: '© 2025 Slovak Inline Hockey Association (SAILH). All rights reserved.',
    }
  }
};

// Vytvorenie vlastného React Contextu pre preklady
const AppContext = createContext();

const AppProvider = ({ children }) => {
  // Stav pre jazyk s ukladaním do localStorage
  const [lang, setLang] = useState(localStorage.getItem('lang') || 'sk');
  
  // Stav pre tmavý režim s ukladaním do localStorage
  const [dark, setDark] = useState(() => localStorage.getItem('theme') === 'dark');

  useEffect(() => {
    localStorage.setItem('lang', lang);
  }, [lang]);
  
  useEffect(() => {
    if (dark) {
      document.documentElement.classList.add('dark');
      localStorage.setItem('theme', 'dark');
    } else {
      document.documentElement.classList.remove('dark');
      localStorage.setItem('theme', 'light');
    }
  }, [dark]);

  const changeLanguage = (newLang) => {
    setLang(newLang);
  };
  
  const toggleDark = () => {
    setDark(!dark);
  };

  // Funkcia 't' (translate) na preklad bežných reťazcov
  const t = (key) => {
    return translations[lang]?.translation[key] || key;
  };
  
  // Funkcia 'thtml' na preklad reťazcov, ktoré obsahujú HTML
  const thtml = (key) => {
      return { __html: translations[lang]?.translation[key] || key };
  };

  return (
    <AppContext.Provider value={{ lang, changeLanguage, t, thtml, dark, toggleDark }}>
      {children}
    </AppContext.Provider>
  );
};

// Vlastný 'useApp' hook, ktorý používa náš Context
const useApp = () => useContext(AppContext);


// --- Pomocné funkcie a Hooky ---

// Hook na scrollovanie hore pri zmene stránky
const ScrollToTop = () => {
  const { pathname } = useLocation();

  useEffect(() => {
    window.scrollTo({ top: 0, behavior: 'smooth' });
  }, [pathname]);

  return null;
};

// --- Hlavné Komponenty Stránky ---

// Hlavička
const Header = () => {
  const { t, lang: currentLanguage, changeLanguage, dark, toggleDark } = useApp();
  const [isMobileMenuOpen, setIsMobileMenuOpen] = useState(false);
  const [isCompetitionsOpen, setIsCompetitionsOpen] = useState(false);
  const location = useLocation();

  const closeMobileMenu = () => setIsMobileMenuOpen(false);

  const NavLink = ({ to, children, className, ...props }) => {
    const isActive = location.pathname === to;
    return (
      <Link
        to={to}
        className={cn(
          "font-semibold transition-colors text-sm",
          isActive ? "text-red-600 dark:text-red-500" : "text-white hover:text-red-500 dark:text-gray-200 dark:hover:text-red-500",
          className
        )}
        onClick={closeMobileMenu}
        {...props}
      >
        {children}
      </Link>
    );
  };

  const MobileNavLink = ({ to, children, icon: Icon, ...props }) => {
    const isActive = location.pathname === to;
    return (
      <Link
        to={to}
        className={cn(
          "flex items-center gap-3 rounded-lg px-3 py-2 transition-all",
          isActive ? "bg-gray-700 text-white" : "text-gray-300 hover:bg-gray-700 hover:text-white"
        )}
        onClick={closeMobileMenu}
        {...props}
      >
        <Icon className="h-4 w-4" />
        {children}
      </Link>
    );
  };

  return (
    <header className="sticky top-0 z-50 w-full border-b border-gray-700 bg-blue-900/90 backdrop-blur-md dark:bg-gray-900/90 dark:border-gray-700">
      <div className="container mx-auto flex h-16 max-w-7xl items-center justify-between px-4 md:px-6">
        {/* Logo a Názov */}
        <Link to="/" className="flex items-center gap-3" onClick={closeMobileMenu}>
          <img
            src="https://www.sailh.sk/logo.png"
            onError={(e) => { e.target.onerror = null; e.target.src = 'https://placehold.co/40x40/ffffff/244b94?text=SAILH'; }}
            alt="SAILH Logo"
            className="h-10 w-10 object-contain"
          />
          <span className="hidden text-xl font-bold text-white sm:inline-block">SAILH</span>
        </Link>

        {/* Desktop Navigácia */}
        <nav className="hidden items-center gap-4 lg:gap-6 md:flex">
          <NavLink to="/">{t('NAV_HOME')}</NavLink>
          <NavLink to="/o-nas">{t('NAV_ABOUT')}</NavLink>
          {/* Desktop Dropdown */}
          <div className="relative group">
            <button className="flex items-center gap-1 font-semibold text-sm text-white hover:text-red-500 transition-colors dark:text-gray-200 dark:hover:text-red-500">
              {t('NAV_COMPETITIONS')} <ChevronDown className="h-4 w-4" />
            </button>
            <div className="absolute left-0 top-full z-10 mt-2 w-48 origin-top-left scale-95 transform-gpu rounded-md bg-white p-1 opacity-0 shadow-lg ring-1 ring-black ring-opacity-5 transition-all duration-200 ease-out group-hover:scale-100 group-hover:opacity-100 dark:bg-gray-800 dark:ring-gray-700">
              <Link to="/extraliga" className="flex items-center gap-2 px-3 py-2 text-sm text-gray-700 hover:bg-gray-100 rounded-md dark:text-gray-200 dark:hover:bg-gray-700">
                <Shield className="h-4 w-4" /> {t('NAV_EXTRALIGA')}
              </Link>
              <Link to="/ial" className="flex items-center gap-2 px-3 py-2 text-sm text-gray-700 hover:bg-gray-100 rounded-md dark:text-gray-200 dark:hover:bg-gray-700">
                <Users className="h-4 w-4" /> {t('NAV_IAL')}
              </Link>
            </div>
          </div>
          <NavLink to="/uspechy">{t('NAV_SUCCESSES')}</NavLink>
          <NavLink to="/partneri">{t('NAV_PARTNERS')}</NavLink>
          <NavLink to="/media">{t('NAV_MEDIA')}</NavLink>
          <NavLink to="/kalendar">{t('NAV_CALENDAR')}</NavLink>
        </nav>

        {/* Pravá strana - Kontakt, Jazyk, Mobilné menu */}
        <div className="flex items-center gap-3">
          <Link to="/kontakt" className="hidden sm:inline-block">
            <Button variant="destructive" size="sm">
              {t('NAV_CONTACT')}
            </Button>
          </Link>
          {/* Prepínač Jazyka */}
          <div className="flex gap-1">
            <Button
              variant="ghost"
              size="icon"
              className={cn(
                "h-8 w-8 text-white",
                currentLanguage === 'sk' ? 'bg-red-600' : 'hover:bg-blue-800 dark:hover:bg-gray-700'
              )}
              onClick={() => changeLanguage('sk')}
            >
              <span className="text-xs font-bold">SK</span>
            </Button>
            <Button
              variant="ghost"
              size="icon"
              className={cn(
                "h-8 w-8 text-white",
                currentLanguage === 'en' ? 'bg-red-600' : 'hover:bg-blue-800 dark:hover:bg-gray-700'
              )}
              onClick={() => changeLanguage('en')}
            >
              <span className="text-xs font-bold">EN</span>
            </Button>
          </div>
          {/* Tlačidlo Tmavého Režimu */}
          <Button
            variant="ghost"
            size="icon"
            className="text-white hover:bg-blue-800 dark:hover:bg-gray-700 h-8 w-8"
            onClick={toggleDark}
          >
            {dark ? <Sun className="h-4 w-4" /> : <Moon className="h-4 w-4" />}
          </Button>
          {/* Mobilné Menu Button */}
          <Button
            variant="ghost"
            size="icon"
            className="text-white hover:bg-blue-800 md:hidden h-8 w-8"
            onClick={() => setIsMobileMenuOpen(!isMobileMenuOpen)}
          >
            {isMobileMenuOpen ? <X className="h-5 w-5" /> : <Menu className="h-5 w-5" />}
          </Button>
        </div>
      </div>

      {/* Mobilné Menu Panel */}
      {isMobileMenuOpen && (
        <div className="absolute top-16 left-0 z-40 w-full border-t border-gray-700 bg-blue-900 p-4 md:hidden dark:bg-gray-900">
          <nav className="flex flex-col gap-2">
            <MobileNavLink to="/" icon={Home}>{t('NAV_HOME')}</MobileNavLink>
            <MobileNavLink to="/o-nas" icon={Info}>{t('NAV_ABOUT')}</MobileNavLink>
            {/* Mobilný Akordeón pre Súťaže */}
            <div>
              <button
                className="flex w-full items-center justify-between rounded-lg px-3 py-2 text-gray-300 hover:bg-gray-700 hover:text-white"
                onClick={() => setIsCompetitionsOpen(!isCompetitionsOpen)}
              >
                <span className="flex items-center gap-3">
                  <Trophy className="h-4 w-4" />
                  {t('NAV_COMPETITIONS')}
                </span>
                <ChevronDown
                  className={cn(
                    "h-4 w-4 transition-transform",
                    isCompetitionsOpen ? "rotate-180" : ""
                  )}
                />
              </button>
              {isCompetitionsOpen && (
                <div className="mt-1 ml-7 flex flex-col gap-1 border-l border-gray-600 pl-4 dark:border-gray-500">
                  <MobileNavLink to="/extraliga" icon={Shield}>{t('NAV_EXTRALIGA')}</MobileNavLink>
                  <MobileNavLink to="/ial" icon={Users}>{t('NAV_IAL')}</MobileNavLink>
                </div>
              )}
            </div>
            <MobileNavLink to="/uspechy" icon={Medal}>{t('NAV_SUCCESSES')}</MobileNavLink>
            <MobileNavLink to="/partneri" icon={Handshake}>{t('NAV_PARTNERS')}</MobileNavLink>
            <MobileNavLink to="/media" icon={Newspaper}>{t('NAV_MEDIA')}</MobileNavLink>
            <MobileNavLink to="/kalendar" icon={CalendarDays}>{t('NAV_CALENDAR')}</MobileNavLink>
            <Link to="/kontakt" className="mt-2" onClick={closeMobileMenu}>
              <Button variant="destructive" className="w-full">
                {t('NAV_CONTACT')}
              </Button>
            </Link>
          </nav>
        </div>
      )}
    </header>
  );
};

// Pätička
const Footer = () => {
  const { t } = useApp();
  return (
    <footer className="bg-blue-900 text-gray-300 dark:bg-gray-900">
      <div className="container mx-auto max-w-7xl px-4 py-12 md:px-6">
        <div className="grid grid-cols-1 gap-12 md:grid-cols-3">
          {/* O nás */}
          <div className="md:col-span-1">
            <div className="flex items-center gap-3">
              <img
                src="https://www.sailh.sk/logo.png"
                onError={(e) => { e.target.onerror = null; e.target.src = 'https://placehold.co/40x40/ffffff/244b94?text=SAILH'; }}
                alt="SAILH Logo"
                className="h-10 w-10 object-contain"
              />
              <span className="text-xl font-bold text-white">SAILH</span>
            </div>
            <p className="mt-4 text-sm">
              {t('FOOTER_ABOUT')}
            </p>
            <div className="mt-6 flex gap-4">
              <a href="https://www.facebook.com/sailhsvk/" target="_blank" rel="noopener noreferrer" className="text-gray-400 hover:text-white">
                <Facebook className="h-6 w-6" />
                <span className="sr-only">Facebook</span>
              </a>
              <a href="https://www.youtube.com/@HOCKEYTV-LIVE" target="_blank" rel="noopener noreferrer" className="text-gray-400 hover:text-white">
                <Youtube className="h-6 w-6" />
                <span className="sr-only">YouTube</span>
              </a>
            </div>
          </div>
          {/* Navigácia */}
          <div>
            <h4 className="font-semibold text-white">{t('FOOTER_NAV_TITLE')}</h4>
            <ul className="mt-4 space-y-2 text-sm">
              <li><Link to="/o-nas" className="hover:text-white">{t('NAV_ABOUT')}</Link></li>
              <li><Link to="/extraliga" className="hover:text-white">{t('NAV_EXTRALIGA')}</Link></li>
              <li><Link to="/ial" className="hover:text-white">{t('NAV_IAL')}</Link></li>
              <li><Link to="/partneri" className="hover:text-white">{t('NAV_PARTNERS')}</Link></li>
              <li><Link to="/kontakt" className="hover:text-white">{t('NAV_CONTACT')}</Link></li>
            </ul>
          </div>
          {/* Kontakt */}
          <div>
            <h4 className="font-semibold text-white">{t('FOOTER_CONTACT_TITLE')}</h4>
            <ul className="mt-4 space-y-3 text-sm">
              <li className="flex items-start gap-3">
                <MapPin className="h-4 w-4 flex-shrink-0 text-gray-400 mt-1" />
                <span>{t('CONTACT_ADDRESS')}</span>
              </li>
              <li className="flex items-start gap-3">
                <Phone className="h-4 w-4 flex-shrink-0 text-gray-400 mt-1" />
                <a href={`tel:${t('CONTACT_PHONE')}`} className="hover:text-white">{t('CONTACT_PHONE')}</a>
              </li>
              <li className="flex items-start gap-3">
                <Mail className="h-4 w-4 flex-shrink-0 text-gray-400 mt-1" />
                <a href={`mailto:${t('CONTACT_EMAIL')}`} className="hover:text-white">{t('CONTACT_EMAIL')}</a>
              </li>
            </ul>
          </div>
        </div>
        <div className="mt-12 border-t border-gray-700 pt-8 text-center text-sm">
          <p>{t('FOOTER_COPYRIGHT')}</p>
        </div>
      </div>
    </footer>
  );
};

// --- Komponenty pre špecifické stránky ---

// Hrdina (Hero) na domovskej stránke
const Hero = () => {
  const { t, thtml } = useApp();
  return (
    <div className="relative -mt-16 h-[60vh] min-h-[400px] w-full overflow-hidden">
      {/* Fallback pre video */}
      <div className="absolute inset-0 bg-blue-900">
        <img 
          src="https://www.sailh.sk/content/images/2025/08/a96c5d80-9eb5-42a4-8c01-28867d2557e4-2.jpg"
          alt="Inline hokej"
          className="w-full h-full object-cover opacity-30"
          onError={(e) => { e.target.onerror = null; e.target.src = 'https://placehold.co/1920x800/244b94/ffffff?text=SAILH'; }}
        />
      </div>
      <video
        autoPlay
        loop
        muted
        playsInline
        className="absolute inset-0 z-0 h-full w-full object-cover"
        poster="https://www.sailh.sk/content/images/2025/08/a96c5d80-9eb5-42a4-8c01-28867d2557e4-2.jpg"
        onError={(e) => { e.target.style.display = 'none'; }}
      >
        <source src="https://www.sailh.sk/video/banner.webm" type="video/webm" />
        <source src="https://www.sailh.sk/video/banner.mp4" type="video/mp4" />
      </video>
      <div className="absolute inset-0 z-10 bg-black/60" />
      <div className="relative z-20 flex h-full flex-col items-center justify-center text-center text-white">
        <div className="container px-4 md:px-6">
          <h1
            className="text-4xl font-extrabold tracking-tighter sm:text-5xl md:text-6xl lg:text-7xl"
            dangerouslySetInnerHTML={thtml('HERO_TITLE')}
          />
          <p className="mx-auto mt-4 max-w-[700px] text-lg text-gray-200 md:text-xl dark:text-gray-300">
            {t('HERO_SUBTITLE')}
          </p>
          <Link to="/o-nas" className="mt-8">
            <Button size="lg" variant="destructive">
              {t('HERO_BUTTON')} <ChevronRight className="ml-2 h-5 w-5" />
            </Button>
          </Link>
        </div>
      </div>
    </div>
  );
};

// RSS Feed komponent
const RssFeed = () => {
  const { t } = useApp();
  const [feed, setFeed] = useState(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);

  // URL pre RSS a proxy
  const RSS_FEED_URL = 'https://www.mihwa.org/feed/';
  const CORS_PROXY = 'https://api.rss2json.com/v1/api.json?rss_url=';

  useEffect(() => {
    const fetchFeed = async () => {
      setLoading(true);
      setError(null);
      try {
        const response = await fetch(CORS_PROXY + encodeURIComponent(RSS_FEED_URL));
        if (!response.ok) {
          throw new Error('Network response was not ok');
        }
        const data = await response.json();
        if (data.status !== 'ok' || !data.items) {
          throw new Error('Failed to parse RSS feed');
        }
        setFeed(data.items.slice(0, 4)); // Zobraziť prvé 4 položky
      } catch (err) {
        setError(err.message);
        console.error("RSS Fetch Error:", err);
      }
      setLoading(false);
    };

    fetchFeed();
  }, []);

  return (
    <Card className="shadow-xl bg-white/70 backdrop-blur-md dark:bg-gray-800/60 dark:backdrop-blur-md dark:border-gray-700/50">
      <CardHeader>
        <CardTitle className="text-red-600">{t('RSS_TITLE')}</CardTitle>
        <CardDescription>{t('RSS_SOURCE')}</CardDescription>
      </CardHeader>
      <CardContent>
        <div className="space-y-4">
          {loading && (
            <div className="flex items-center gap-2 text-gray-500 dark:text-gray-400">
              <Activity className="h-4 w-4 animate-spin" />
              <span>{t('RSS_LOADING')}</span>
            </div>
          )}
          {error && (
            <div className="flex items-center gap-2 text-red-600">
              <X className="h-4 w-4" />
              <span>{t('RSS_ERROR')}</span>
            </div>
          )}
          {feed && feed.map((item, index) => (
            <div key={index} className="border-b pb-3 last:border-b-0 dark:border-gray-600">
              <a
                href={item.link}
                target="_blank"
                rel="noopener noreferrer"
                className="font-semibold text-blue-800 hover:underline dark:text-blue-300"
              >
                {item.title}
              </a>
              <p className="mt-1 text-xs text-gray-500 dark:text-gray-400">
                {new Date(item.pubDate).toLocaleDateString('sk-SK')}
              </p>
            </div>
          ))}
        </div>
      </CardContent>
    </Card>
  );
};

// Komponent pre sekciu stránky
const PageSection = ({ title, children, className }) => (
  <div className={cn("container mx-auto max-w-7xl px-4 py-16 md:px-6", className)}>
    <h2 className="mb-4 text-center text-4xl font-extrabold tracking-tight text-blue-900 dark:text-gray-100">
      {title}
    </h2>
    <div className="mx-auto mt-3 h-1.5 w-24 rounded-full bg-red-600" />
    <div className="mt-12">
      {children}
    </div>
  </div>
);

// --- Jednotlivé Stránky ---

// Domovská stránka
const HomePage = () => {
  const { t } = useApp();
  return (
    <>
      <Hero />
      <div className="container mx-auto max-w-7xl px-4 py-16 md:px-6">
        <div className="grid grid-cols-1 gap-12 lg:grid-cols-3">
          {/* Ľavý stĺpec - Aktuality */}
          <div className="lg:col-span-2 space-y-8">
            <h2 className="text-4xl font-extrabold tracking-tight text-blue-900 dark:text-gray-100">
              {t('HOME_NEWS_TITLE')}
            </h2>
            
            {/* Pridané dôležité odkazy/tlačidlá */}
            <div className="flex flex-col sm:flex-row gap-4">
              <a href="https://www.inlineliga.sk/" target="_blank" rel="noopener noreferrer" className="w-full">
                <Button variant="secondary" size="lg" className="w-full gap-2 text-base">
                  <Globe className="h-5 w-5" /> {t('LINK_IAL')}
                </Button>
              </a>
              <a href="https://www.mihwa.org/" target="_blank" rel="noopener noreferrer" className="w-full">
                <Button variant="secondary" size="lg" className="w-full gap-2 text-base">
                  <Trophy className="h-5 w-5" /> {t('LINK_MIHWA')}
                </Button>
              </a>
            </div>
            
            <Card className="overflow-hidden shadow-xl transition-shadow duration-300 hover:shadow-2xl">
              <div className="grid grid-cols-1 md:grid-cols-3">
                <img
                  src="slovak_plotter.jpg"
                  onError={(e) => { e.target.onerror = null; e.target.src = 'https://placehold.co/300x200/244b94/ffffff?text=Majstri+2025'; }}
                  alt="Majstri Extraligy"
                  className="h-48 w-full object-cover md:h-full"
                />
                <div className="p-6 md:col-span-2">
                  <span className="text-xs font-bold uppercase text-red-600">
                    {t('NEWS_1_CATEGORY')}
                  </span>
                  <h3 className="mt-1 text-xl font-semibold text-blue-900 dark:text-gray-100">
                    {t('NEWS_1_TITLE')}
                  </h3>
                  <p className="mt-2 text-sm text-gray-600 dark:text-gray-400">
                    {t('NEWS_1_DESC')}
                  </p>
                  <Link to="/uspechy">
                    <Button variant="link" className="px-0 mt-3 text-xs">
                      {t('READ_MORE')} <ChevronRight className="ml-1 h-4 w-4" />
                    </Button>
                  </Link>
                </div>
              </div>
            </Card>

            <Card className="overflow-hidden shadow-xl transition-shadow duration-300 hover:shadow-2xl">
              <div className="grid grid-cols-1 md:grid-cols-3">
                <img
                  src="https://placehold.co/300x200/007bff/ffffff?text=ME+Bronz"
                  alt="ME Bronz"
                  className="h-48 w-full object-cover md:h-full"
                />
                <div className="p-6 md:col-span-2">
                  <span className="text-xs font-bold uppercase text-blue-600">
                    {t('NEWS_2_CATEGORY')}
                  </span>
                  <h3 className="mt-1 text-xl font-semibold text-blue-900 dark:text-gray-100">
                    {t('NEWS_2_TITLE')}
                  </h3>
                  <p className="mt-2 text-sm text-gray-600 dark:text-gray-400">
                    {t('NEWS_2_DESC')}
                  </p>
                  <Link to="/uspechy">
                    <Button variant="link" className="px-0 mt-3 text-xs">
                      {t('READ_MORE')} <ChevronRight className="ml-1 h-4 w-4" />
                    </Button>
                  </Link>
                </div>
              </div>
            </Card>
          </div>

          {/* Pravý stĺpec - RSS a Video */}
          <div className="space-y-8">
            <RssFeed />
            <Card className="shadow-xl">
              <CardHeader>
                <CardTitle className="text-blue-900 dark:text-gray-100">{t('VIDEO_TITLE')}</CardTitle>
              </CardHeader>
              <CardContent>
                <div className="aspect-video w-full overflow-hidden rounded-lg">
                  <iframe
                    className="w-full h-full"
                    src="https://www.youtube.com/embed/CwQp_DjAlTw?controls=1&rel=0" // Aktualizované video
                    frameBorder="0"
                    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
                    allowFullScreen
                    title="NITRA HOCKEY TEAM vs ILK ROLLER HAMMERS BRATISLAVA"
                    loading="lazy"
                  ></iframe>
                </div>
                <p className="mt-3 text-sm text-gray-600 dark:text-gray-400">{t('VIDEO_DESC')}</p>
              </CardContent>
            </Card>
          </div>
        </div>
      </div>
    </>
  );
};

// Komponent pre riadok s oficiálnymi údajmi
const InfoRow = ({ label, value, icon: Icon }) => (
  <div className="py-4 sm:grid sm:grid-cols-3 sm:gap-4 sm:py-5">
    <dt className="flex items-center text-sm font-medium text-gray-500 dark:text-gray-400">
      <Icon className="h-4 w-4 mr-2 flex-shrink-0" />
      {label}
    </dt>
    <dd className="mt-1 text-sm text-gray-900 sm:mt-0 sm:col-span-2 dark:text-gray-100">{value}</dd>
  </div>
);

// Stránka O nás
const AboutPage = () => {
  const { t, thtml } = useApp();
  
  const priorities = [
    { icon: Sparkles, title: t('ABOUT_PRIORITY_1_TITLE'), text: t('ABOUT_PRIORITY_1_TEXT') },
    { icon: Trophy, title: t('ABOUT_PRIORITY_2_TITLE'), text: t('ABOUT_PRIORITY_2_TEXT') },
    { icon: Shield, title: t('ABOUT_PRIORITY_3_TITLE'), text: t('ABOUT_PRIORITY_3_TEXT') },
  ];
  
  // Oficiálne údaje
  const officialData = [
    { icon: FileText, label: t('ABOUT_LEGAL_NAME'), value: t('ABOUT_LEGAL_NAME_VALUE') },
    { icon: Briefcase, label: t('ABOUT_LEGAL_FORM'), value: t('ABOUT_LEGAL_FORM_VALUE') },
    { icon: Hash, label: t('ABOUT_ICO'), value: t('ABOUT_ICO_VALUE') },
    { icon: MapPin, label: t('ABOUT_ADDRESS'), value: t('ABOUT_ADDRESS_VALUE') },
    { icon: FileText, label: t('ABOUT_REG_NUM'), value: t('ABOUT_REG_NUM_VALUE') },
    { icon: Building, label: t('ABOUT_REG_AUTH'), value: t('ABOUT_REG_AUTH_VALUE') },
    { icon: CalendarDays, label: t('ABOUT_DATE'), value: t('ABOUT_DATE_VALUE') },
  ];

  return (
    <PageSection title={t('ABOUT_TITLE')}>
      <Card className="max-w-4xl mx-auto shadow-2xl">
        <CardContent className="p-6 md:p-10">
          <p className="text-lg text-gray-700 dark:text-gray-300" dangerouslySetInnerHTML={thtml('ABOUT_P1')} />

          <h3 className="mt-10 text-2xl font-semibold text-blue-900 dark:text-gray-100">{t('ABOUT_MISSION_TITLE')}</h3>
          <div className="mt-6 grid grid-cols-1 md:grid-cols-2 gap-8">
            <div className="rounded-lg bg-blue-50 p-6 border-l-4 border-blue-700 dark:bg-blue-900/20 dark:border-blue-600">
              <h4 className="text-xl font-bold text-blue-800 dark:text-blue-200">{t('ABOUT_MISSION')}</h4>
              <p className="mt-2 text-gray-700 dark:text-gray-300">{t('ABOUT_MISSION_TEXT')}</p>
            </div>
            <div className="rounded-lg bg-red-50 p-6 border-l-4 border-red-600 dark:bg-red-900/20 dark:border-red-600">
              <h4 className="text-xl font-bold text-red-700 dark:text-red-300">{t('ABOUT_VISION')}</h4>
              <p className="mt-2 text-gray-700 dark:text-gray-300">{t('ABOUT_VISION_TEXT')}</p>
            </div>
          </div>

          <h3 className="mt-10 text-2xl font-semibold text-blue-900 dark:text-gray-100">{t('ABOUT_PRIORITIES_TITLE')}</h3>
          <div className="mt-6 space-y-4">
            {priorities.map((item, index) => (
              <div key={index} className="flex items-start gap-4">
                <div className="flex-shrink-0 rounded-full bg-blue-700 p-3 text-white dark:bg-blue-600">
                  <item.icon className="h-5 w-5" />
                </div>
                <div>
                  <h4 className="text-lg font-semibold dark:text-gray-100">{item.title}</h4>
                  <p className="text-gray-600 dark:text-gray-400">{item.text}</p>
                </div>
              </div>
            ))}
          </div>

          {/* Nová sekcia: Oficiálne údaje */}
          <h3 className="mt-10 text-2xl font-semibold text-blue-900 dark:text-gray-100">{t('ABOUT_OFFICIAL_DATA_TITLE')}</h3>
          <Card className="mt-6 border-t-4 border-blue-700 dark:border-blue-600">
            <CardContent className="p-0">
              <dl className="divide-y divide-gray-200 dark:divide-gray-700">
                {officialData.map((item) => (
                  <InfoRow key={item.label} label={item.label} value={item.value} icon={item.icon} />
                ))}
              </dl>
            </CardContent>
          </Card>
          
        </CardContent>
      </Card>
    </PageSection>
  );
};

// Stránka Extraliga
const ExtraligaPage = () => {
  const { t } = useApp();
  
  const teams = [
    { rank: 1, name: t('TEAM_1_NAME'), games: 18, points: 48 },
    { rank: 2, name: t('TEAM_2_NAME'), games: 18, points: 40 },
    { rank: 3, name: t('TEAM_3_NAME'), games: 18, points: 35 },
    { rank: 4, name: t('TEAM_4_NAME'), games: 18, points: 31 },
  ];

  return (
    <PageSection title={t('EXTRALIGA_TITLE')}>
      <Card className="max-w-4xl mx-auto shadow-2xl">
        <CardContent className="p-6 md:p-10">
          <p className="text-lg text-gray-700 text-center dark:text-gray-300">{t('EXTRALIGA_DESC')}</p>
          
          <h3 className="mt-10 text-2xl font-semibold text-blue-900 dark:text-gray-100">Tabuľka (Simulovaná)</h3>
          <div className="mt-6 overflow-x-auto rounded-lg border dark:border-gray-700">
            <table className="min-w-full divide-y divide-gray-200 dark:divide-gray-700">
              <thead className="bg-gray-100 dark:bg-gray-700">
                <tr>
                  <th scope="col" className="px-6 py-3 text-left text-xs font-bold text-blue-900 uppercase tracking-wider dark:text-blue-200">{t('TABLE_RANK')}</th>
                  <th scope="col" className="px-6 py-3 text-left text-xs font-bold text-blue-900 uppercase tracking-wider dark:text-blue-200">{t('TABLE_TEAM')}</th>
                  <th scope="col" className="px-6 py-3 text-left text-xs font-bold text-blue-900 uppercase tracking-wider dark:text-blue-200">{t('TABLE_GAMES')}</th>
                  <th scope="col" className="px-6 py-3 text-left text-xs font-bold text-blue-900 uppercase tracking-wider dark:text-blue-200">{t('TABLE_POINTS')}</th>
                </tr>
              </thead>
              <tbody className="bg-white divide-y divide-gray-200 dark:bg-gray-800 dark:divide-gray-700">
                {teams.map((team) => (
                  <tr key={team.rank} className="hover:bg-gray-50 dark:hover:bg-gray-700">
                    <td className="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900 dark:text-gray-100">{team.rank}.</td>
                    <td className="px-6 py-4 whitespace-nowrap text-sm font-semibold text-gray-800 dark:text-gray-200">{team.name}</td>
                    <td className="px-6 py-4 whitespace-nowrap text-sm text-gray-600 dark:text-gray-400">{team.games}</td>
                    <td className="px-6 py-4 whitespace-nowrap text-sm font-bold text-gray-900 dark:text-gray-100">{team.points}</td>
                  </tr>
                ))}
              </tbody>
            </table>
          </div>
        </CardContent>
      </Card>
    </PageSection>
  );
};

// Stránka IAL
const IalPage = () => {
  const { t } = useApp();
  return (
    <PageSection title={t('IAL_TITLE')}>
      <Card className="max-w-5xl mx-auto shadow-2xl">
        <CardContent className="p-6 md:p-10">
          <p className="text-lg text-gray-700 text-center dark:text-gray-300">{t('IAL_DESC')}</p>
          <p className="mt-4 text-center text-sm text-red-600 bg-red-50 p-3 rounded-md dark:bg-red-900/20 dark:text-red-300">
            <Info className="inline-block h-4 w-4 mr-2" />
            {t('IAL_NOTE')}
          </p>

          <h3 className="mt-10 text-2xl font-semibold text-blue-900 dark:text-gray-100">{t('IAL_CURRENT_MATCHES')}</h3>
          <div className="mt-6 grid grid-cols-1 md:grid-cols-2 gap-8">
            <Card className="border-blue-600 border-t-4">
              <CardHeader>
                <CardTitle className="text-xl text-blue-800 dark:text-blue-300">{t('IAL1_TITLE')}</CardTitle>
                <CardDescription>{t('IAL_DATE_1')}</CardDescription>
              </CardHeader>
              <CardContent className="space-y-3">
                <div className="flex justify-between text-sm">
                  <span className="text-gray-500 dark:text-gray-400">17:00</span>
                  <span className="font-medium dark:text-gray-100">{t('IAL1_MATCH_1')}</span>
                </div>
                <div className="flex justify-between text-sm">
                  <span className="text-gray-500 dark:text-gray-400">18:15</span>
                  <span className="font-medium dark:text-gray-100">{t('IAL1_MATCH_2')}</span>
                </div>
                <div className="flex justify-between text-sm italic">
                  <span className="text-gray-500 dark:text-gray-400">{t('IAL_BYE')}:</span>
                  <span className="font-medium dark:text-gray-100">{t('IAL_TEAM_BEEWARE')}</span>
                </div>
              </CardContent>
            </Card>

            <Card className="border-red-600 border-t-4">
              <CardHeader>
                <CardTitle className="text-xl text-red-800 dark:text-red-400">{t('IAL2_TITLE')}</CardTitle>
                <CardDescription>{t('IAL_DATE_2')}</CardDescription>
              </CardHeader>
              <CardContent className="space-y-3">
                <div className="flex justify-between text-sm">
                  <span className="text-gray-500 dark:text-gray-400">19:30</span>
                  <span className="font-medium dark:text-gray-100">{t('IAL2_MATCH_1')}</span>
                </div>
                <div className="flex justify-between text-sm">
                  <span className="text-gray-500 dark:text-gray-400">20:30</span>
                  <span className="font-medium dark:text-gray-100">{t('IAL2_MATCH_2')}</span>
                </div>
              </CardContent>
            </Card>
          </div>
        </CardContent>
      </Card>
    </PageSection>
  );
};

// Stránka Úspechy
const SuccessesPage = () => {
  const { t } = useApp();
  return (
    <PageSection title={t('SUCCESSES_TITLE')}>
      <p className="text-lg text-gray-700 text-center max-w-3xl mx-auto dark:text-gray-300">{t('SUCCESSES_DESC')}</p>
      
      <div className="mt-12 max-w-4xl mx-auto space-y-10">
        <Card className="overflow-hidden shadow-2xl">
          <img
            src="https://placehold.co/800x400/d90f23/ffffff?text=Majstri+Sveta+Juniori"
            alt="Juniori Majstri Sveta"
            className="w-full h-64 object-cover"
          />
          <CardHeader>
            <span className="text-sm font-bold uppercase text-red-600">{t('SUCCESS_1_CATEGORY')}</span>
            <CardTitle className="mt-1 text-3xl text-blue-900 dark:text-gray-100">{t('SUCCESS_1_TITLE')}</CardTitle>
          </CardHeader>
          <CardContent className="space-y-4">
            <p className="text-lg font-medium text-gray-800 dark:text-gray-200">{t('SUCCESS_1_P1')}</p>
            <p className="text-gray-600 dark:text-gray-400">{t('SUCCESS_1_P2')}</p>
          </CardContent>
        </Card>
        
        <Card className="overflow-hidden shadow-2xl">
          <img
            src="https://placehold.co/800x400/244b94/ffffff?text=ME+Striebro+Seniori"
            alt="Seniori Striebro"
            className="w-full h-64 object-cover"
          />
          <CardHeader>
            <span className="text-sm font-bold uppercase text-blue-700 dark:text-blue-400">{t('SUCCESS_2_CATEGORY')}</span>
            <CardTitle className="mt-1 text-3xl text-blue-900 dark:text-gray-100">{t('SUCCESS_2_TITLE')}</CardTitle>
          </CardHeader>
          <CardContent>
            <p className="text-lg font-medium text-gray-800 dark:text-gray-200">{t('SUCCESS_2_P1')}</p>
          </CardContent>
        </Card>
      </div>
    </PageSection>
  );
};

// Stránka Partneri
const PartnersPage = () => {
  const { t } = useApp();
  return (
    <PageSection title={t('PARTNERS_TITLE')}>
      <p className="text-lg text-gray-700 text-center max-w-3xl mx-auto dark:text-gray-300">{t('PARTNERS_DESC')}</p>
      
      <Card className="max-w-5xl mx-auto mt-12 shadow-2xl">
        <CardContent className="p-6 md:p-10">
          {/* Generálny partner */}
          <h3 className="text-2xl font-semibold text-blue-900 text-center dark:text-gray-100">{t('PARTNERS_GENERAL')}</h3>
          <div className="mt-8 flex justify-center">
            <img 
              src="https://upload.wikimedia.org/wikipedia/commons/thumb/1/14/Tipos_logo.svg/200px-Tipos_logo.svg.png" 
              alt="TIPOS Logo" 
              className="max-h-24"
            />
          </div>
          
          {/* Hlavní partneri */}
          <h3 className="mt-12 text-2xl font-semibold text-blue-900 text-center dark:text-gray-100">{t('PARTNERS_MAIN')}</h3>
          <div className="mt-8 grid grid-cols-2 md:grid-cols-3 gap-8 items-center">
            <div className="flex justify-center">
              <img src="https://hustlehockey.com/wp-content/uploads/2021/08/logo.svg" alt="HUSTLE HOCKEY Logo" className="max-h-16 opacity-70 grayscale transition-all duration-300 hover:opacity-100 hover:grayscale-0 dark:invert dark:opacity-80 dark:hover:opacity-100" />
            </div>
            <div className="flex justify-center">
              <img src="https://www.slovakplotter.sk/images/logo.png" alt="Slovak Plotter Logo" className="max-h-20 opacity-70 grayscale transition-all duration-300 hover:opacity-100 hover:grayscale-0 dark:invert dark:opacity-80 dark:hover:opacity-100" />
            </div>
            <div className="flex justify-center">
              <img src="https://placehold.co/200x100/f0f0f0/333333?text=Partner+3" alt="Partner 3 Logo" className="max-h-16 opacity-70 grayscale transition-all duration-300 hover:opacity-100 hover:grayscale-0 dark:invert dark:opacity-80 dark:hover:opacity-100" />
            </div>
          </div>
          
          {/* Mediálni partneri */}
          <h3 className="mt-12 text-2xl font-semibold text-blue-900 text-center dark:text-gray-100">{t('PARTNERS_MEDIA')}</h3>
          <div className="mt-8 grid grid-cols-2 md:grid-cols-2 gap-8 items-center max-w-md mx-auto">
            <div className="flex justify-center">
              <img src="https://placehold.co/200x100/f0f0f0/333333?text=Mediálny+Partner+A" alt="Mediálny Partner A Logo" className="max-h-12 opacity-70 grayscale transition-all duration-300 hover:opacity-100 hover:grayscale-0 dark:invert dark:opacity-80 dark:hover:opacity-100" />
            </div>
            <div className="flex justify-center">
              <img src="https://placehold.co/200x100/f0f0f0/333333?text=Mediálny+Partner+B" alt="Mediálny Partner B Logo" className="max-h-12 opacity-70 grayscale transition-all duration-300 hover:opacity-100 hover:grayscale-0 dark:invert dark:opacity-80 dark:hover:opacity-100" />
            </div>
          </div>
          
          {/* Výzva */}
          <div className="mt-16 rounded-lg bg-gray-100 p-8 text-center shadow-inner dark:bg-gray-700">
            <h4 className="text-2xl font-bold text-blue-900 dark:text-gray-100">{t('PARTNERS_CTA_TITLE')}</h4>
            <p className="mt-2 text-gray-700 max-w-lg mx-auto dark:text-gray-300">{t('PARTNERS_CTA_DESC')}</p>
            <Link to="/kontakt" className="mt-6">
              <Button variant="destructive">
                <Handshake className="mr-2 h-4 w-4" /> {t('PARTNERS_CTA_BUTTON')}
              </Button>
            </Link>
          </div>
        </CardContent>
      </Card>
    </PageSection>
  );
};

// Stránka Médiá
const MediaPage = () => {
  const { t } = useApp();
  // Aktualizované videá na reálne zápasy
  const videos = [
    { id: 'CwQp_DjAlTw', title: 'NITRA HOCKEY TEAM vs ILK ROLLER HAMMERS BRATISLAVA' },
    { id: 's0sA4vT7TC0', title: 'Finále Extraligy 2024: HAMMERS vs PRUSKÉ (Záznam)' },
    { id: 'T2Wd-6H-p-Q', title: 'Finále Extraligy 2024: Zostrih' },
  ];
  const posters = [
    { img: 'https://placehold.co/400x600/244b94/ffffff?text=Turnaj+Plagát', alt: 'Turnaj Plagát' },
    { img: 'https://placehold.co/400x600/d90f23/ffffff?text=Extraliga+2025', alt: 'Extraliga Plagát' },
    { img: 'https://placehold.co/400x600/007bff/ffffff?text=Mládežnícky+Kemp', alt: 'Mládežnícky Kemp Plagát' },
  ];

  return (
    <PageSection title={t('MEDIA_TITLE')}>
      <p className="text-lg text-gray-700 text-center max-w-3xl mx-auto dark:text-gray-300">{t('MEDIA_DESC')}</p>
      
      <h3 className="mt-12 text-3xl font-semibold text-blue-900 dark:text-gray-100">{t('MEDIA_VIDEO_GALLERY')}</h3>
      <div className="mt-8 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {videos.map((video) => (
          <Card key={video.id} className="overflow-hidden shadow-xl transition-shadow duration-300 hover:shadow-2xl">
            <div className="aspect-video w-full">
              <iframe
                className="w-full h-full"
                src={`https://www.youtube.com/embed/${video.id}?controls=1&rel=0`}
                frameBorder="0"
                allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
                allowFullScreen
                title={video.title}
                loading="lazy"
              ></iframe>
            </div>
          </Card>
        ))}
      </div>
      
      <h3 className="mt-16 text-3xl font-semibold text-blue-900 dark:text-gray-100">{t('MEDIA_POSTERS_GALLERY')}</h3>
      <div className="mt-8 grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-8">
        {posters.map((poster, index) => (
          <Card key={index} className="overflow-hidden shadow-xl group">
            <img 
              src={poster.img} 
              alt={poster.alt} 
              className="w-full h-auto object-cover transition-transform duration-300 group-hover:scale-105"
            />
          </Card>
        ))}
      </div>
    </PageSection>
  );
};

// Stránka Kalendár
const CalendarPage = () => {
  const { t } = useApp();
  return (
    <PageSection title={t('CALENDAR_TITLE')}>
      <p className="text-lg text-gray-700 text-center max-w-3xl mx-auto dark:text-gray-300">{t('CALENDAR_DESC')}</p>
      <Card className="max-w-5xl mx-auto mt-12 shadow-2xl overflow-hidden">
        <iframe
          src="https://calendar.google.com/calendar/embed?height=600&wkst=2&ctz=Europe%2FPrague&bgcolor=%23ffffff&src=sk.slovak.hockey%40gmail.com&color=%235CB3AA&showTitle=0&showPrint=0&showTabs=1&showCalendars=0&showTz=0"
          style={{ border: 0 }}
          width="100%"
          height="600"
          frameBorder="0"
          scrolling="no"
          title="SAILH Google Calendar"
        ></iframe>
      </Card>
    </PageSection>
  );
};

// Stránka Kontakt
const ContactPage = () => {
  const { t } = useApp();
  const [formSubmitted, setFormSubmitted] = useState(false);

  const handleSubmit = (e) => {
    e.preventDefault();
    // Tu by bola logika na odoslanie formulára
    setFormSubmitted(true);
    setTimeout(() => {
      setFormSubmitted(false);
      e.target.reset();
    }, 5000);
  };

  const contactDetails = [
    { icon: MapPin, text: t('CONTACT_ADDRESS'), href: '#' },
    { icon: Phone, text: t('CONTACT_PHONE'), href: `tel:${t('CONTACT_PHONE')}` },
    { icon: Mail, text: t('CONTACT_EMAIL'), href: `mailto:${t('CONTACT_EMAIL')}` },
    { icon: Globe, text: 'sailh.sk', href: 'https://sailh.sk' },
  ];
  
  return (
    <PageSection title={t('CONTACT_TITLE')}>
      <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 max-w-6xl mx-auto">
        {/* Kontaktný formulár */}
        <Card className="shadow-2xl">
          <CardHeader>
            <CardTitle className="text-blue-900 dark:text-gray-100">{t('CONTACT_FORM_TITLE')}</CardTitle>
          </CardHeader>
          <CardContent>
            {formSubmitted ? (
              <div className="flex flex-col items-center justify-center h-full min-h-[300px] text-center p-6 bg-green-50 rounded-md border border-green-300 dark:bg-green-900/20 dark:border-green-700">
                <div className="w-16 h-16 rounded-full bg-green-600 flex items-center justify-center text-white">
                  <Send className="h-8 w-8" />
                </div>
                <h3 className="mt-4 text-2xl font-semibold text-green-800 dark:text-green-200">{t('CONTACT_FORM_SUCCESS')}</h3>
              </div>
            ) : (
              <form onSubmit={handleSubmit} className="space-y-4">
                <div>
                  <label htmlFor="name" className="block text-sm font-medium text-gray-700 dark:text-gray-300">{t('CONTACT_FORM_NAME')}</label>
                  <Input id="name" type="text" required className="mt-1" />
                </div>
                <div>
                  <label htmlFor="email" className="block text-sm font-medium text-gray-700 dark:text-gray-300">{t('CONTACT_FORM_EMAIL')}</label>
                  <Input id="email" type="email" required className="mt-1" />
                </div>
                <div>
                  <label htmlFor="message" className="block text-sm font-medium text-gray-700 dark:text-gray-300">{t('CONTACT_FORM_MESSAGE')}</label>
                  <Textarea id="message" rows="5" required className="mt-1" />
                </div>
                <div>
                  <Button type="submit" variant="destructive" className="w-full">
                    {t('CONTACT_FORM_SUBMIT')} <Send className="ml-2 h-4 w-4" />
                  </Button>
                </div>
              </form>
            )}
          </CardContent>
        </Card>
        
        {/* Kontaktné info a sociálne siete */}
        <div className="space-y-8">
          <Card className="shadow-2xl">
            <CardHeader>
              <CardTitle className="text-blue-900 dark:text-gray-100">{t('CONTACT_DETAILS_TITLE')}</CardTitle>
            </CardHeader>
            <CardContent className="space-y-4">
              {contactDetails.map((item, index) => (
                <div key={index} className="flex items-start gap-4">
                  <div className="flex-shrink-0 text-blue-700 mt-1 dark:text-blue-400">
                    <item.icon className="h-5 w-5" />
                  </div>
                  <a href={item.href} target="_blank" rel="noopener noreferrer" className="text-gray-700 hover:text-blue-700 hover:underline dark:text-gray-300 dark:hover:text-blue-400">
                    {item.text}
                  </a>
                </div>
              ))}
            </CardContent>
          </Card>

          <Card className="shadow-2xl">
            <CardHeader>
              <CardTitle className="text-blue-900 dark:text-gray-100">{t('CONTACT_SOCIAL_TITLE')}</CardTitle>
            </CardHeader>
            <CardContent>
              <div className="flex gap-4">
                <a href="https://www.facebook.com/sailhsvk/" target="_blank" rel="noopener noreferrer">
                  <Button variant="outline" size="lg" className="flex-1 gap-2">
                    <Facebook className="h-5 w-5 text-blue-700" /> Facebook
                  </Button>
                </a>
                 <a href="https://www.youtube.com/@HOCKEYTV-LIVE" target="_blank" rel="noopener noreferrer">
                  <Button variant="outline" size="lg" className="flex-1 gap-2">
                    <Youtube className="h-5 w-5 text-red-600" /> YouTube
                  </Button>
                </a>
              </div>
              {/* Aktualizovaná sekcia pre FB Feed */}
              <div className="mt-6 w-full overflow-hidden rounded-lg border dark:border-gray-700">
                <iframe
                  src="https://www.facebook.com/plugins/page.php?href=https%3A%2F%2Fwww.facebook.com%2Fsailhsvk&tabs=timeline&width=500&height=600&small_header=false&adapt_container_width=true&hide_cover=false&show_facepile=true"
                  width="500"
                  height="600"
                  style={{ border: 'none', overflow: 'hidden' }}
                  scrolling="no"
                  frameBorder="0"
                  allowFullScreen={true}
                  allow="autoplay; clipboard-write; encrypted-media; picture-in-picture; web-share"
                  className="w-full max-w-[500px] mx-auto"
                  loading="lazy"
                ></iframe>
              </div>
            </CardContent>
          </Card>
        </div>
      </div>
    </PageSection>
  );
};

// Nový komponent pre tlačidlo "Späť na vrch"
const BackToTopButton = () => {
  const [isVisible, setIsVisible] = useState(false);

  const toggleVisibility = () => {
    if (window.scrollY > 300) {
      setIsVisible(true);
    } else {
      setIsVisible(false);
    }
  };

  const scrollToTop = () => {
    window.scrollTo({
      top: 0,
      behavior: 'smooth',
    });
  };

  useEffect(() => {
    window.addEventListener('scroll', toggleVisibility);
    return () => {
      window.removeEventListener('scroll', toggleVisibility);
    };
  }, []);

  return (
    <div className="fixed bottom-6 right-6 z-50">
      {isVisible && (
        <Button
          onClick={scrollToTop}
          variant="destructive"
          size="icon"
          className="rounded-full shadow-lg h-12 w-12"
        >
          <ArrowUp className="h-6 w-6" />
        </Button>
      )}
    </div>
  );
};


// --- Hlavná App Komponent ---
export default function App() {
  return (
    <AppProvider>
      <HashRouter>
        <GlobalStyle /> {/* Pridaný globálny štýl pre písmo */}
        <div className="flex min-h-screen flex-col bg-gray-50 text-gray-900 dark:bg-gray-900 dark:text-gray-50">
          <Header />
          <ScrollToTop />
            <main className="flex-grow pt-16">
              <Routes>
                <Route path="/" element={<HomePage />} />
                <Route path="/o-nas" element={<AboutPage />} />
                <Route path="/extraliga" element={<ExtraligaPage />} />
                <Route path="/ial" element={<IalPage />} />
                <Route path="/uspechy" element={<SuccessesPage />} />
                <Route path="/partneri" element={<PartnersPage />} />
                <Route path="/media" element={<MediaPage />} />
                <Route path="/kalendar" element={<CalendarPage />} />
                <Route path="/kontakt" element={<ContactPage />} />
              </Routes>
          </main>
          <Footer />
          <BackToTopButton />
        </div>
      </HashRouter>
    </AppProvider>
  );
}

